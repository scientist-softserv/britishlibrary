<!-- rendered from views/hyrax/base///attribute_rows_custom.html.erb -->

<% array_of_hash = get_model(presenter.editor, presenter.model, 'editor', 'editor_position') %>

<% if (array_of_hash.class == Array && array_of_hash.first.class == Hash)  %>
  <%= render "shared/ubiquity/editor/show_array_hash",  array_of_hash: array_of_hash %>
<% else %>

<% end %> <%# closes if array_of_hash.class %>
